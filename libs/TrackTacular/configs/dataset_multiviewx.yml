data:
  class_path: datasets.dataloaders.PedestrianDataLoader
  init_args:
    data_dir: "C:/Users/Mr. RIAH/Documents/Projects/chikamera/datasets/MultiviewX"
    data_type: multiviewx
model:
  resolution: [160, 2, 250]  # Y,Z,X
  bounds: [0, 1000, 0, 640, 0, 2]  # xmin,xmax,ymin,ymax,zmin,zmax
  depth: [32, 0, -20]
  z_sign: -1
  num_cameras: 6
trainer:
  max_epochs: 50
